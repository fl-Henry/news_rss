const puppeteer = require('puppeteer');


class RssNews {

    constructor(url) {
        this.url = url;
    }

    createBrowser() {

    }
}

// Usage example:
const instance = new MyClass();
instance.myMethod("Hello, World!");